import { Box, Paper } from "@mui/material";
import * as React from "react";

export interface LoginPageProps {}

export function LoginPage(props: LoginPageProps) {
  return (
    <Box>
      <Paper elevation={4}>Login</Paper>
    </Box>
  );
}
